Rounded surface container. 16px radius, soft navy-tinted shadow.

```jsx
<Card>Default white card</Card>
<Card variant="navy" hoverable>Feature on dark</Card>
<Card variant="tint" padding={32}>Highlighted block</Card>
```

Variants: `default` (white + shadow), `flat` (white, border only), `navy` (dark sections), `tint` (blue wash). Set `hoverable` for interactive lift.
